Corrija **somente o erro de compilação Android identificado no último build EAS**. Não implemente nenhuma outra melhoria ou alteração funcional.

### Erro exato do Gradle

O build falhou em:

`:app:processDebugMainManifest`

com:

`Manifest merger failed`

Existe conflito entre:

* `@six33/react-native-bg-removal`

  * declara `com.google.mlkit.vision.DEPENDENCIES`
  * valor: `subject_segment`

* `expo-dev-launcher`

  * declara `com.google.mlkit.vision.DEPENDENCIES`
  * valor: `barcode_ui`

O próprio Gradle sugere:

`tools:replace="android:value"`

### Objetivo

Resolver **somente esse conflito de AndroidManifest**, garantindo que a configuração necessária para:

`@six33/react-native-bg-removal@1.3.4`

continue prevalecendo, especialmente:

`com.google.mlkit.vision.DEPENDENCIES = subject_segment`

Não remova, substitua ou atualize:

`@six33/react-native-bg-removal@1.3.4`

### Regras obrigatórias

1. Não altere `app/index.tsx`.
2. Não altere a lógica de remoção de fundo.
3. Não altere `ImageManipulator`.
4. Não altere `AsyncStorage`.
5. Não altere as camadas.
6. Não altere a composição Parallax.
7. Não altere o editor ou suas dimensões.
8. Não atualize o Expo SDK.
9. Não atualize `@six33/react-native-bg-removal`.
10. Não instale nenhuma biblioteca nova além do que já foi configurado para o Development Build.
11. Não remova `expo-dev-client`.
12. Não altere funcionalidades do aplicativo.
13. Não faça uma nova tentativa de `eas build` ainda.

### Procedimento

Primeiro localize como o projeto Expo SDK 54 está gerando/aplicando o AndroidManifest durante o Development Build.

Implemente a solução mínima e correta para que o Manifest Merger aceite os dois manifests e preserve `subject_segment`.

Se for necessário criar ou alterar um config plugin, faça somente o plugin/configuração estritamente necessário para esse conflito.

Depois:

* valide o TypeScript;
* valide a configuração Expo;
* valide que o plugin/configuração Android é aceito;
* examine o diff para confirmar que somente a correção do conflito foi feita.

**Não gere um novo APK nesta etapa.**

Ao terminar, informe exatamente:

1. quais arquivos foram alterados;
2. qual alteração foi feita para resolver o conflito;
3. como a configuração garante a permanência de `subject_segment`;
4. quais validações foram executadas;
5. confirme explicitamente que nenhum novo build EAS foi iniciado.
