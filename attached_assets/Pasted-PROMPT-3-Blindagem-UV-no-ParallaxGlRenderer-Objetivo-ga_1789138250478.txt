PROMPT 3 — Blindagem UV no ParallaxGlRenderer
Objetivo: garantir que, se após a correção EXIF ainda sobrar divergência entre a proporção física da textura e a proporção declarada em imageWidth:imageHeight, o crop seja ajustado cortando bordas em vez de esticar o conteúdo. Nunca distorcer, em nenhuma hipótese.

Pré-requisitos: Prompts 1 e 2 já aplicados.

Arquivo: android/app/src/main/java/com/parallaxwallpaper/app/ParallaxGlRenderer.java

Alteração — método updateLayerUvTransform
Localize o método updateLayerUvTransform e substitua-o integralmente.

Método atual:

java
  private void updateLayerUvTransform(int index) {
    ParallaxTextureManager.LayerData layer = textureManager.getLayerData(index);
    if (layer == null
        || !layer.hasSourceCrop
        || layer.imageWidth <= 0f
        || layer.imageHeight <= 0f) {
      mUvTransform[0] = 0f;
      mUvTransform[1] = 0f;
      mUvTransform[2] = 1f;
      mUvTransform[3] = 1f;
      return;
    }
    mUvTransform[0] = layer.cropOriginX / layer.imageWidth;
    mUvTransform[1] = layer.cropOriginY / layer.imageHeight;
    mUvTransform[2] = layer.cropWidth / layer.imageWidth;
    mUvTransform[3] = layer.cropHeight / layer.imageHeight;
  }
Substituir por:

java
  private void updateLayerUvTransform(int index) {
    ParallaxTextureManager.LayerData layer = textureManager.getLayerData(index);
    if (layer == null
        || !layer.hasSourceCrop
        || layer.imageWidth <= 0f
        || layer.imageHeight <= 0f) {
      mUvTransform[0] = 0f;
      mUvTransform[1] = 0f;
      mUvTransform[2] = 1f;
      mUvTransform[3] = 1f;
      return;
    }

    float imageAspect = layer.imageWidth / layer.imageHeight;
    float cropAspect = layer.cropWidth / layer.cropHeight;

    // UV nominal em espaço da imagem.
    float u0 = layer.cropOriginX / layer.imageWidth;
    float v0 = layer.cropOriginY / layer.imageHeight;
    float du = layer.cropWidth / layer.imageWidth;
    float dv = layer.cropHeight / layer.imageHeight;

    // Se a proporção física da textura corresponde à proporção declarada da imagem
    // (o caso saudável após EXIF), usamos o UV nominal.
    // Caso contrário, ajustamos o UV para preservar a proporção do crop,
    // cortando bordas em vez de esticar o conteúdo.
    if (layer.textureWidth > 0 && layer.textureHeight > 0) {
      float textureAspect = (float) layer.textureWidth / (float) layer.textureHeight;
      if (Math.abs(textureAspect - imageAspect) > imageAspect * 0.01f) {
        // Extensão física do crop em pixels da textura.
        float cropPixelsW = layer.cropWidth * ((float) layer.textureWidth / layer.imageWidth);
        float cropPixelsH = layer.cropHeight * ((float) layer.textureHeight / layer.imageHeight);

        if (cropPixelsW > 0f && cropPixelsH > 0f) {
          float physicalAspect = cropPixelsW / cropPixelsH;
          if (physicalAspect > cropAspect) {
            // Textura "larga demais" para o crop — encolher em X, centrado.
            float newPixelW = cropPixelsH * cropAspect;
            float deltaPixels = (cropPixelsW - newPixelW) / 2f;
            u0 += deltaPixels / (float) layer.textureWidth;
            du = newPixelW / (float) layer.textureWidth;
          } else if (physicalAspect < cropAspect) {
            // Textura "alta demais" para o crop — encolher em Y, centrado.
            float newPixelH = cropPixelsW / cropAspect;
            float deltaPixels = (cropPixelsH - newPixelH) / 2f;
            v0 += deltaPixels / (float) layer.textureHeight;
            dv = newPixelH / (float) layer.textureHeight;
          }
        }
      }
    }

    mUvTransform[0] = u0;
    mUvTransform[1] = v0;
    mUvTransform[2] = du;
    mUvTransform[3] = dv;
  }
Restrições
Não alterar mais nada no arquivo.

Não alterar updateLayerMvp — a geometria do quad permanece como está, dimensionada pelo crop.

Não alterar o shader.

Não alterar updateSensorState.

Não alterar validateAlphaPrecision, AlphaPrecisionGlState ou AlphaPrecisionCase.

Não alterar ParallaxTextureManager, CanvasWallpaperRenderer, ParallaxEglController, ParallaxEglThread ou ParallaxWallpaperService.

A tolerância 0.01f (1%) é intencional: evita micro-ajustes em diferenças numéricas irrelevantes.

Justificativa da abordagem
O método antigo assumia silenciosamente que textureWidth/textureHeight == imageWidth/imageHeight. Quando essa igualdade falhava (por EXIF não aplicado, decoder com arredondamento diferente, ou JSON desalinhado com o arquivo), as UVs mapeavam uma região da textura com proporção diferente da declarada, e a GPU esticava o resultado — alongando em X ou Y conforme o caso.

O novo método verifica essa igualdade primeiro:

Se vale (caso saudável depois do Prompt 2), o comportamento é idêntico ao anterior.

Se não vale, ele preserva a proporção do crop cortando bordas simetricamente, em vez de esticar. Isso garante que nenhuma foto do usuário seja distorcida, mesmo em cenários anômalos que escapem do EXIF.

A escolha por corte centralizado (em vez de corte em uma borda) evita enviesamento visual e mantém o enquadramento mais próximo do que o usuário definiu no editor.

Verificação esperada após o patch
Compilação sem erros.

Nenhum novo log introduzido pelo método — ele é silencioso por design.

Logs de sanidade continuam aparecendo:

PARALLAX_EXIF_APPLIED index=N orientation=X width=W height=H — quando EXIF foi aplicado.

PARALLAX_GL_BITMAP_READY — com width × height coerentes com as dimensões visuais.

PARALLAX_GL_ALPHA_PRECISION_VALIDATION PASSED — sanidade do GL.

Em teste nos dois OPPO A78:

Nenhuma foto retrato/paisagem, com EXIF 1/6/8, com crop quadrado ou retangular, deve apresentar alongamento visível.

Alternar entre Canvas e GL (se o seletor ainda existir nesta build) deve mostrar o mesmo enquadramento nos dois modos.

Movimentação suave do GL deve ser preservada.