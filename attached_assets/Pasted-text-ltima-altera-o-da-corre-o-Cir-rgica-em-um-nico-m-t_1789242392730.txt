text
Última alteração da correção. Cirúrgica em um único método.

ARQUIVO: ParallaxGlRenderer.java
MÉTODO: updateLayerUvTransform(int index)

Hoje o método começa assim:

    private void updateLayerUvTransform(int index) {
      ParallaxTextureManager.LayerData layer = textureManager.getLayerData(index);
      if (layer == null
          || !layer.hasSourceCrop
          || layer.imageWidth <= 0f
          || layer.imageHeight <= 0f) {
        mUvTransform[0] = 0f;
        mUvTransform[1] = 0f;
        mUvTransform[2] = 1f;
        mUvTransform[3] = 1f;
        return;
      }

      float imageAspect = layer.imageWidth / layer.imageHeight;
      ...

ALTERAÇÃO:
Inserir um novo bloco de verificação LOGO APÓS a leitura de `layer` e ANTES do if existente. O bloco trata o caso em que a textura já foi recortada pelo editor.

CÓDIGO A INSERIR (exatamente assim, com indentação de 4 espaços alinhada ao método):

      if (layer.textureIsCropped) {
        if (BuildConfig.DEBUG && layer.textureWidth > 0) {
          AppLog.i(
              "PARALLAX_GL_CROPPED_TEXTURE index=" + index
                  + " texture=" + layer.textureWidth + "x" + layer.textureHeight
                  + " crop=" + layer.cropWidth + "x" + layer.cropHeight);
        }
        mUvTransform[0] = 0f;
        mUvTransform[1] = 0f;
        mUvTransform[2] = 1f;
        mUvTransform[3] = 1f;
        return;
      }

POSICIONAMENTO EXATO:
O novo bloco deve ficar imediatamente após esta linha:

    ParallaxTextureManager.LayerData layer = textureManager.getLayerData(index);

E imediatamente antes desta linha:

    if (layer == null
        || !layer.hasSourceCrop

Ou seja: a leitura do `layer` acontece primeiro, depois o novo bloco `if (layer.textureIsCropped)`, depois o if existente `if (layer == null || !layer.hasSourceCrop || ...)`.

CONTEXTO E REGRAS:
- Quando `textureIsCropped` for true, o bitmap JÁ contém o recorte fisicamente. Aplicar `sourceCrop` como UV amostraria uma sub-região errada do bitmap — é exatamente esse bug que estamos corrigindo.
- Nesse caso, a UV correta é a textura inteira: [0,1] x [0,1].
- O bloco de ajuste de aspecto que existe mais abaixo no método (`if (Math.abs(textureAspect - imageAspect) > ...)`) NÃO deve rodar neste caso — o `return` garante isso, o que é o comportamento desejado.
- O `return` também impede que o método continue a processar o resto, o que é intencional.
- NÃO alterar o if existente (`if (layer == null || !layer.hasSourceCrop ...)`).
- NÃO alterar o bloco de ajuste de aspecto mais abaixo.
- NÃO alterar nenhum outro método.
- NÃO alterar `updateLayerMvp` — ele está correto e deve permanecer intacto.

RESULTADO ESPERADO:
- UM bloco `if (layer.textureIsCropped)` inserido entre a leitura de `layer` e o if existente.
- Nenhuma outra modificação em nenhum arquivo.

Por favor, mostre o diff antes de aplicar e não faça mais nada além disso.
O que verificar no diff antes de aprovar:

Um bloco if (layer.textureIsCropped) novo, contendo o AppLog.i e as quatro atribuições a mUvTransform, terminando com return;.

Posicionado exatamente entre a linha ParallaxTextureManager.LayerData layer = ... e o if (layer == null || !layer.hasSourceCrop ...).

O if existente continua intacto logo abaixo.

O bloco de ajuste de aspecto mais abaixo no método continua intacto.

Nada mais tocado no arquivo.